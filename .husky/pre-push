#!/usr/bin/env sh

# Keep local pushes aligned with CI so formatting or build regressions stop
# before they reach GitHub Actions.
npm test && npm run typecheck && npm run lint && npm run format:check && npm run build
