export default function Home() {
  return (
    <main className="shell">
      <p className="eyebrow">M0 · Engineering baseline</p>
      <h1>Multiplayer Canvas</h1>
      <p className="lede">A learning-first foundation for a real-time collaborative technical diagramming app.</p>
      <section className="status-card" aria-labelledby="status-title">
        <h2 id="status-title">The workspace is ready for the curriculum.</h2>
        <p>Collaboration, persistence, authentication, and the canvas editor are intentionally deferred until their lessons begin.</p>
      </section>
    </main>
  );
}
